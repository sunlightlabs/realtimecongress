# used only in house_live
:services:
  :shared_secret:
  :api_name: realtimecongress

s3:
    secret:
    key:

debug: false

log_file: /dev/null

:email:
  :via: :smtp
  :to:
    -
  :from:
  :via_options:
    :address:
    :port:
    :user_name:
    :password:
    :authentication: plain
    :domain:
    :enable_starttls_auto: false

# to send emails to owners of specific tasks
:task_owners:
  # :bills_archive:
  #   - email@example.com